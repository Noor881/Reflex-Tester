{
  "name": "reflex-tester",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "check": "node --check main.js && node --check nav.js && node --check tool-runtime.js && node scripts/validate-site.mjs",
    "smoke": "node scripts/browser-smoke.mjs",
    "build": "npm run check"
  }
}
