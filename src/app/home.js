import { tools } from '../data/tools.js';
import { store } from '../core/store.js';
import { mountShell } from './shell.js';
mountShell('');
const recent=store.get('recent',[]).map(slug=>tools.find(tool=>tool.slug===slug)).filter(Boolean);
const pick=slug=>tools.find(tool=>tool.slug===slug);
const rows=items=>items.map(tool=>`<a class="tool-row" href=".${tool.url}"><span class="tool-kind">${tool.category}</span><span class="tool-name">${tool.name}</span><p>${tool.description}</p><span class="tool-arrow" aria-hidden="true">→</span></a>`).join('');
document.querySelector('#main').innerHTML=`<div class="page"><section class="home-intro"><div><span class="eyebrow">Performance tests and practice</span><h1>Measure first. Practice with purpose.</h1><p class="lede">Reaction tests, mouse-control drills, cognitive exercises and game setup utilities. No account required.</p><div style="display:flex;gap:.7rem;flex-wrap:wrap;margin-top:1.5rem"><a class="button accent" href="./tools/visual-reflex-test.html">Test reaction time</a><a class="button secondary" href="./tools.html">Browse all tools</a></div></div><aside class="quick-test surface"><span class="eyebrow">Start here</span><h2>Visual reaction test</h2><div class="instrument-readout">5<span class="instrument-unit">attempts</span></div><p>Click when the field changes. Your best and average are saved locally.</p><div class="quick-test-actions"><a class="button accent" href="./tools/visual-reflex-test.html">Begin test</a></div></aside></section>${recent.length?`<section class="section-block"><div class="section-heading"><div><span class="eyebrow">Continue</span><h2>Recently used</h2></div><p>Stored only in this browser.</p></div><div class="tool-list">${rows(recent.slice(0,4))}</div></section>`:''}<section class="section-block"><div class="section-heading"><div><span class="eyebrow">Measure</span><h2>Run a focused test</h2></div><p>Use repeatable conditions and compare several attempts.</p></div><div class="tool-list">${rows([pick('visual-reflex-test'),pick('click-speed-test'),pick('typing-speed-test'),pick('peripheral-vision-test')])}</div></section><section class="section-block"><div class="section-heading"><div><span class="eyebrow">Practice</span><h2>Train mouse control</h2></div><p>Short drills for flicking, tracking, target switching and precision.</p></div><div class="tool-list">${rows([pick('gridshot-arena'),pick('tracking-challenge'),pick('precision-aim-test'),pick('valorant-aim-trainer')])}</div></section><section class="section-block"><div class="section-heading"><div><span class="eyebrow">Configure</span><h2>Game utilities</h2></div><p>Calculate, preview and save repeatable settings.</p></div><div class="compact-grid">${[pick('sensitivity-calculator'),pick('csgo-crosshair-generator'),pick('cod-loadout-builder')].map(tool=>`<a class="compact-link surface" href=".${tool.url}"><span class="tool-kind">${tool.category}</span><strong>${tool.name}</strong><span aria-hidden="true">→</span></a>`).join('')}</div></section><section class="section-block"><div class="section-heading"><div><span class="eyebrow">Your data</span><h2>Results stay on this device</h2></div><p>Review recent attempts, personal bests and averages. Clear everything whenever you want.</p></div><a class="button secondary" href="./dashboard.html">Open results</a></section></div>`;
