import { tools } from '../data/tools.js';
import { mountShell } from './shell.js';
const params=new URLSearchParams(location.search);let section=params.get('section')||'all';mountShell(section==='all'?'':section);
const main=document.querySelector('#main');main.innerHTML=`<div class="page"><header class="catalog-head"><div><span class="eyebrow">Tool directory</span><h1>Find the right instrument.</h1><p class="lede">Search by task or narrow the list to tests, training or utilities.</p></div><div class="catalog-controls"><label class="field"><span>Search tools</span><input type="search" data-search placeholder="Reaction, tracking, Valorant…"></label></div></header><div class="segmented" aria-label="Tool type">${[['all','All'],['test','Tests'],['train','Training'],['utility','Utilities']].map(([id,label])=>`<button class="segment" data-section="${id}" aria-pressed="${section===id}">${label}</button>`).join('')}</div><p class="result-count" data-count></p><div class="tool-list" data-list></div><div class="empty-state" data-empty hidden>No tools match that search.</div></div>`;
const search=main.querySelector('[data-search]'),list=main.querySelector('[data-list]'),count=main.querySelector('[data-count]'),empty=main.querySelector('[data-empty]');
function render(){const query=search.value.trim().toLowerCase();const filtered=tools.filter(tool=>(section==='all'||tool.section===section)&&`${tool.name} ${tool.category} ${tool.description}`.toLowerCase().includes(query));count.textContent=`${filtered.length} ${filtered.length===1?'tool':'tools'}`;list.innerHTML=filtered.map(tool=>`<a class="tool-row" href=".${tool.url}"><span class="tool-kind">${tool.category}</span><span class="tool-name">${tool.name}</span><p>${tool.description}</p><span class="tool-arrow">→</span></a>`).join('');empty.hidden=filtered.length!==0;}
search.oninput=render;main.querySelectorAll('[data-section]').forEach(button=>button.onclick=()=>{section=button.dataset.section;main.querySelectorAll('[data-section]').forEach(item=>item.setAttribute('aria-pressed',String(item===button)));history.replaceState({},'',section==='all'?'tools.html':`tools.html?section=${section}`);render();});render();
